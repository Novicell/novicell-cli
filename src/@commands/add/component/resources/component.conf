<template>
  <div>component</div>
</template>

<script lang="ts">
import { Component, Vue, Prop } from 'nuxt-property-decorator';

@Component({
  components: {
  },
})
export default class TheFooter extends Vue {
  data: any;

  @Prop({
    default: {},
  })

  get getter_example(): string {
    return 'text from a getter (computed value)'
  }
}
</script>

<style scoped>
</style>
